interface:
  display_name: "Test UI"
  short_description: "Run scripted UI tests and compare console output"
  default_prompt: "Run the command-line UI tests listed in test/ui-test-plan.md and show the complete console transcript. Stop at the first failure."
